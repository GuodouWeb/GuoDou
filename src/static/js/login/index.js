import { addevent } from './app'
import { getLocalStorageAccount,querySearch,login_btn, register_btn } from './loginFunction'
import {account, accountUserName, register, defaultImg} from './loginData'

export {
    getLocalStorageAccount,
    querySearch,
    addevent,
    account,
    accountUserName,
    register,
    defaultImg,
    login_btn,
    register_btn
}
